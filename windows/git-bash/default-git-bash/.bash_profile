# ~/.bash_profile

# Load Git Bash interactive configuration
if [ -f "$HOME/.bashrc" ]; then
    source "$HOME/.bashrc"
fi

# cat > ~/.bash_profile <<'EOF'
# # ~/.bash_profile

# # Load Git Bash interactive configuration
# if [ -f "$HOME/.bashrc" ]; then
#     source "$HOME/.bashrc"
# fi
# EOF