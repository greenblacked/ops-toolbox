- `install_apps.sh` grew `--list-casks` and `--list-formulae`, the same
  shape as `install_devtools.sh --list-tools`. The catalogue adds
  Obsidian and Stats, plus `gh`, `sops`, `age`, `vault`, `packer`,
  `cloud-sql-proxy`, `fzf`, `ripgrep`, `fd`, `k6`, `shellcheck` and
  `hadolint`.
